# ============================================================================
# Finans Takip - Ortam Değişkenleri Örneği
# ============================================================================
# Bu dosyayı .env olarak kopyalayın ve değerleri ayarlayın.

# --------------------------------------------------------------------------
# Veritabanı Ayarları
# --------------------------------------------------------------------------
# PostgreSQL (Production önerilir)
DATABASE_URL=postgresql://username:password@localhost:5432/finans_takip

# Alternatif: SQLite (Geliştirme)
# DATABASE_URL=sqlite:///./finans_takip.db

# --------------------------------------------------------------------------
# JWT Token Ayarları
# --------------------------------------------------------------------------
# Secret key: En az 32 karakter, güçlü bir string olmalı
# Üretmek için: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars

# JWT algoritması
ALGORITHM=HS256

# Token geçerlilik süresi (dakika)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --------------------------------------------------------------------------
# CORS Ayarları
# --------------------------------------------------------------------------
# Virgülle ayrılmış allowed origins
# Örnek: http://localhost:3000,https://app.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000

# --------------------------------------------------------------------------
# Ortam Ayarları
# --------------------------------------------------------------------------
# development, staging, production
ENVIRONMENT=development
